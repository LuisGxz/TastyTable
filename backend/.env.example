# TastyTable API — copy to .env for local dev (defaults below match docker-compose)
PORT=3013
MONGODB_URI=mongodb://localhost:27018/tastytable
# JWT (used from F2)
JWT_SECRET=dev-only-change-me-tastytable-secret
JWT_EXPIRES=15m
JWT_REFRESH_EXPIRES=7d
